{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C/C++: Build active file",
            "type": "shell",
            "command": "sh",
            "args": [
                "-c",
                "if [[ \"${file}\" == *.c ]]; then gcc -g \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\"; elif [[ \"${file}\" == *.cpp ]]; then g++ -g \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\"; else echo 'Unsupported file type'; exit 1; fi"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}

