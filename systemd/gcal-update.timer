[Unit]
Description=Run Gcal Update every hour

[Timer]
# Run 1 minute after boot
OnBootSec=1min
# Run every hour on the hour (09:00, 10:00, etc)
OnCalendar=*-*-* *:00:00
# If the computer was off during the last hour, run immediately on wakeup
Persistent=true

[Install]
WantedBy=timers.target
